---
description: Add a Shadcn Component
---

1.  **Ask the user** which component to add (e.g., "button", "dialog").
2.  **Run the CLI command**. Use `npx shadcn@latest add [component]`.
    - // turbo
    - Allows auto-running the command if the user approves.
3.  **Verify Installation**. Check `components/ui/[component].tsx` exists.
4.  **Demonstrate Usage**. Briefly explain or show an example of how to import and use it.
